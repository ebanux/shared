{
  "name": "@ebanux/builder-contract",
  "version": "0.2.3",
  "description": "Versioned wire contract between QRLynk builder hosts and builder runtimes.",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ebanux/shared.git"
  },
  "type": "module",
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/esm/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js",
      "default": "./dist/esm/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "workspaces": [
    "packages/commerce-contract"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.cjs.json",
    "build:commerce": "npm run build --prefix packages/commerce-contract",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:commerce": "npm run typecheck --prefix packages/commerce-contract",
    "test": "npm run build && node --test test/*.test.mjs test/*.test.cjs",
    "test:commerce": "npm test --prefix packages/commerce-contract",
    "verify": "npm run typecheck && npm test && npm pack --dry-run && npm run verify --prefix packages/commerce-contract"
  },
  "devDependencies": {
    "typescript": "5.8.2"
  }
}
